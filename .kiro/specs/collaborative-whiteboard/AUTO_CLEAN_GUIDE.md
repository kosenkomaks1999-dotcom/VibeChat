# Whiteboard Auto-Clean Guide

## 🧹 Автоочистка доски при выходе пользователей

### Проблема

Доска не очищается автоматически когда все пользователи покидают комнату.

---

## ✅ Решение

Добавлен новый метод `forceCleanWhiteboard()` для принудительной очистки доски.

### Два способа очистки:

#### 1. Автоматическая очистка (по неактивности)

**Работает автоматически:**
- Проверка каждые 30 секунд
- Очистка через 5 минут неактивности
- Не требует дополнительного кода

```javascript
// Запускается автоматически при открытии доски
whiteboardManager.open();
```

---

#### 2. Принудительная очистка (при выходе пользователей)

**Нужно вызвать вручную:**

```javascript
// Когда последний пользователь покидает комнату
if (usersInRoom === 0) {
  whiteboardManager.forceCleanWhiteboard();
}
```

---

## 📝 Интеграция в приложение

### Где добавить код

Найдите место в вашем приложении, где отслеживается выход пользователей из комнаты.

**Пример интеграции:**

```javascript
// В app.js или где обрабатывается выход из комнаты

// Слушаем изменения пользователей в комнате
roomRef.child('users').on('value', (snapshot) => {
  const users = snapshot.val();
  const userCount = users ? Object.keys(users).length : 0;
  
  console.log(`Users in room: ${userCount}`);
  
  // Если комната пуста - очищаем доску
  if (userCount === 0 && whiteboardManager) {
    console.log('Room is empty, cleaning whiteboard');
    whiteboardManager.forceCleanWhiteboard();
  }
});
```

---

### Альтернативный вариант (при выходе из комнаты)

```javascript
// Когда пользователь покидает комнату
function leaveRoom() {
  // ... ваш код выхода из комнаты ...
  
  // Проверяем, остались ли еще пользователи
  roomRef.child('users').once('value', (snapshot) => {
    const users = snapshot.val();
    const userCount = users ? Object.keys(users).length : 0;
    
    if (userCount === 0 && whiteboardManager) {
      whiteboardManager.forceCleanWhiteboard();
    }
  });
}
```

---

## 🔧 API методов

### `autoCleanWhiteboard()`

**Назначение:** Автоматическая очистка по неактивности

**Вызов:** Автоматически (каждые 30 сек проверка)

**Условие:** Нет активности 5 минут

```javascript
// Вызывается автоматически, не нужно вызывать вручную
```

---

### `forceCleanWhiteboard()`

**Назначение:** Принудительная очистка при выходе пользователей

**Вызов:** Вручную из вашего кода

**Условие:** Когда в комнате 0 пользователей

```javascript
// Вызывайте когда комната пуста
whiteboardManager.forceCleanWhiteboard();
```

**Что делает:**
1. Останавливает мониторинг активности
2. Очищает локальный массив штрихов
3. Очищает canvas
4. Удаляет все штрихи из Firebase

---

## 🧪 Тестирование

### Тест 1: Автоочистка по неактивности

**Шаги:**
1. Откройте whiteboard
2. Нарисуйте штрихи
3. Подождите 5 минут без активности
4. Через ~30 сек доска очистится

**Ожидаемый результат:**
- Доска очищается автоматически
- В консоли: "Auto-cleaning whiteboard (inactivity detected)"

---

### Тест 2: Принудительная очистка

**Шаги:**
1. Откройте whiteboard
2. Нарисуйте штрихи
3. Все пользователи покидают комнату
4. Вызовите `forceCleanWhiteboard()`

**Ожидаемый результат:**
- Доска очищается немедленно
- В консоли: "Force cleaning whiteboard (room empty)"

---

## 📊 Структура Firebase

Для отслеживания пользователей в комнате используйте:

```
rooms/
  {roomId}/
    users/
      {userId}/
        nickname: "John"
        online: true
        timestamp: 1234567890
    whiteboard/
      strokes/
        {strokeId}/
          ...
```

---

## 💡 Рекомендации

### 1. Отслеживайте пользователей

Убедитесь, что ваше приложение отслеживает количество пользователей в комнате:

```javascript
// Добавляйте пользователя при входе
roomRef.child(`users/${userId}`).set({
  nickname: userNickname,
  online: true,
  timestamp: Date.now()
});

// Удаляйте при выходе
roomRef.child(`users/${userId}`).remove();
```

---

### 2. Используйте onDisconnect

Для надежности используйте Firebase `onDisconnect`:

```javascript
// При входе в комнату
const userRef = roomRef.child(`users/${userId}`);
userRef.set({ nickname, online: true });

// Автоматически удалить при отключении
userRef.onDisconnect().remove();
```

---

### 3. Комбинируйте оба метода

Для максимальной надежности используйте оба подхода:

```javascript
// 1. Автоматическая очистка (работает всегда)
whiteboardManager.open(); // Запускает мониторинг

// 2. Принудительная очистка (при выходе пользователей)
roomRef.child('users').on('value', (snapshot) => {
  const userCount = snapshot.val() ? Object.keys(snapshot.val()).length : 0;
  if (userCount === 0) {
    whiteboardManager.forceCleanWhiteboard();
  }
});
```

---

## ✅ Итог

**Два способа очистки:**

1. **Автоматическая** (по неактивности) - работает всегда ✅
2. **Принудительная** (при выходе пользователей) - нужно интегрировать ✅

**Для полной функциональности:**
- Добавьте отслеживание пользователей в комнате
- Вызывайте `forceCleanWhiteboard()` когда `userCount === 0`
- Используйте `onDisconnect()` для надежности

---

## 📝 Пример полной интеграции

```javascript
// В вашем app.js

let whiteboardManager = null;
let currentRoomRef = null;

// При входе в комнату
function joinRoom(roomId) {
  currentRoomRef = db.ref(`rooms/${roomId}`);
  
  // Создаем whiteboard manager
  whiteboardManager = new WhiteboardManager(
    currentRoomRef,
    myUserId,
    myNickname,
    ui
  );
  
  // Добавляем себя в комнату
  const userRef = currentRoomRef.child(`users/${myUserId}`);
  userRef.set({
    nickname: myNickname,
    online: true,
    timestamp: Date.now()
  });
  
  // Автоудаление при отключении
  userRef.onDisconnect().remove();
  
  // Слушаем изменения пользователей
  currentRoomRef.child('users').on('value', (snapshot) => {
    const users = snapshot.val();
    const userCount = users ? Object.keys(users).length : 0;
    
    console.log(`Users in room: ${userCount}`);
    
    // Очищаем доску если комната пуста
    if (userCount === 0 && whiteboardManager) {
      whiteboardManager.forceCleanWhiteboard();
    }
  });
}

// При выходе из комнаты
function leaveRoom() {
  if (currentRoomRef && myUserId) {
    currentRoomRef.child(`users/${myUserId}`).remove();
  }
  
  if (whiteboardManager) {
    whiteboardManager.close();
    whiteboardManager = null;
  }
}
```

---

**Готово!** Теперь доска будет очищаться как по неактивности, так и при выходе всех пользователей. 🎉
